# Description:	Xfce4 Development tools
# URL:		https://www.xfce.org
# Maintainer:	jolupa <jolupameister(at)gmail[dot](com)
# Depends on:	autoconf automake intltool pkgconfig

name=xfce4-dev-tools
version=4.12.0
release=1
source=()

build() {
	if cd "$name-$version" 2> /dev/null; then
		git pull
	else
		git clone https://git.xfce.org/xfce/$name/ "$name-$version"
		cd "$name-$version"
	fi
	
	./autogen.sh \
	--prefix=/usr \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib \
	--localstatedir=/var \
	--disable-static \
	--disable-gtk-docs
	
	make

	make DESTDIR=$PKG install
}
