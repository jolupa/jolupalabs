# Description:	Xfce's session manager
# URL:		https://www.xfce.org
# Maintainer:	jolupa <jolupameister(at)gmail[dot](com)
# Depends on:	libxfce4ui p5-xml-parser libwnck-gtk3 dbus-glib

name=xfce4-session
version=4.12.0
release=1
source=(https://archive.xfce.org/xfce/4.12/src/$name-$version.tar.bz2)

build() {

	cd $name-$version
	
	./configure \
	--prefix=/usr \
	--libexecdir=/usr/lib/$name \
	--enable-polkit \
	--disable-debug \
	--disable-legacy-sm \
	--with-x

	make

	make DESTDIR=$PKG install
}
