# Description: Thunar-archive-pluginallows you to create and extract archive files using the file context menus in the Thunar file manager.
# URL: https://xfce.org
# Maintainer: jolupa, jlpavon at me dot com
# Depends on: thunar exo libxfce4util

name=thunar-archive-plugin
version=0.4.0
release=1
source=(https://archive.xfce.org/src/thunar-plugins/thunar-archive-plugin/${version:0:3}/thunar-archive-plugin-$version.tar.bz2)

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--libexecdir=/usr/lib/$name

	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/share/locale 
}
