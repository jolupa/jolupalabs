# Description: Gstreamer based media player
# URL: https://docs.xfce.org/apps/parole/introduction 
# Maintainer: jolupa, jlpavon at me dot com
# Depends on: gtk3 glib libxfce4ui libxfce4util xfconf libnotify dbus gstreamer gst-plugins-base gst-plugins-good gst-plugins-bad gst-plugins-ugly

name=parole
version=1.0.5
release=1
source=(https://archive.xfce.org/src/apps/parole/${version:0:3}/$name-$version.tar.bz2)
build() {

  cd $name-$version

  ./configure \
    --prefix=/usr \
    --libexecdir=/usr/lib/$name

  make
  make DESTDIR=$PKG install

  #rm -r $PKG/usr/share/locale
}
