# Description:	Thumbnail service implementing the thumbnail management D-Bus specification
# URL:		https://www.xfce.org
# Maintainer:	jolupa <jolupameister(at)gmail[dot](com)
# Depends on:	dbus-glib glib gdk-pixbuf freetype libjpeg-turbo 

name=tumbler
version=0.1.31
release=1
source=(https://archive.xfce.org/xfce/4.12/src/$name-$version.tar.bz2)

build() {

	cd $name-$version

	./configure \
	  --prefix=/usr \
	  --libexecdir=/usr/lib/$name \
	  --disable-static \
	  --disable-gtk-docs \
	  --disable-gtk2 \
	  --enable-gtk3
	
	make

	make DESTDIR=$PKG install
}
