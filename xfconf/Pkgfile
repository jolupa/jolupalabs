# Description:	xfconf for xfce4 desktop
# URL:		https://xfce.org
# Maintainer:	jolupa <jolupameister(at)gmail(dot)com>
# Depends on:	libxfce4util dbus-glib

name=xfconf
version=4.13.6
release=1
source=(http://archive.xfce.org/src/xfce/$name/${version%.*}/$name-$version.tar.bz2)

build() {
  cd $name-$version
  ./configure \
    --prefix=/usr \
    --sysconfdir=/etc \
    --datadir=/usr/share \
    --sbindir=/usr/bin \
    --libexecdir=/usr/lib \
    --localstatedir=/var \
    --disable-static \
    --disable-gtk-doc \
    --disable-gtk-debug \
    --mandir=/usr/share/man \
    --disable-nls \
    --disable-gtk-doc-html \
    --disable-debug \
    --disable-gsettings-backend
make

make DESTDIR=$PKG install

rm -r $PKG/usr/share/gtk-doc
}
