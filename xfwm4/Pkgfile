# Description: Xfce's window manager
# URL: https://www.xfce.org
# Maintainer: jolupa, jlpavon at me dot com
# Depends on: libxfce4util libxfce4ui xfconf glib libwnck-gtk3 startup-notification

name=xfwm4
version=4.14.0
release=2
source=(http://archive.xfce.org/xfce/4.14/src/$name-$version.tar.bz2)

build() {

  cd $name-$version
	
    ./configure \
      --prefix=/usr \
      --libexecdir=/usr/lib/$name \
      --enable-shared \
      --enable-xsync \
      --enable-render \
      --enable-randr \
      --enable-xpresent \
      --enable-compositor \
      --enable-epoxy \
      --disable-debug

    make
    make DESTDIR=$PKG install
  
    #rm -r $PKG/usr/share/locale
    rm $PKG/usr/share/themes/{Daloa,Default-hdpi,Default-xhdpi,Kokodi,Moheli}/xfwm4/README
}
