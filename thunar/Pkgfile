# Description:	Modern, gast and easy-to-use file manager for Xfce
# URL:		https://www.xfce.org
# Maintainer:	jolupa <jolupameister(at)gmail[dot](com)
# Depends on:	libxfce4ui exo libpng gtk3 glib libexif dbus-glib libnotify startup-notification freetype libjpeg-turbo xfce4-panel xfconf

name=thunar
version=1.8.4
release=1
source=()

build() {
	if cd "$name-$version" 2> /dev/null; then
		git pull
	else
		git clone https://git.xfce.org/xfce/$name/ "$name-$version"
		cd "$name-$version"
	fi
	
	./autogen.sh \
	--prefix=/usr \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib \
	--localstatedir=/var \
	--disable-static \
	--disable-gtk-docs \
	--disable-gtk-2
	make

	make DESTDIR=$PKG install
}
